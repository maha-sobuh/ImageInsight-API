# ImageInsight-API
A REST API for image ingestion, analysis, and AI-powered description. Upload images via URL or Base64, extract metadata (format, size, resolution, histogram), store them in storage, and generate smart captions using an LLM — all in one pipeline.
